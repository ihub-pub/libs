description = 'SSO模块组件'

dependencies {
    compileOnly 'org.springframework.boot:spring-boot'
}

subprojects {
    dependencies {
        api project(':ihub-sso'), project(':ihub-boot-cloud-spring-boot-starter')
    }
}

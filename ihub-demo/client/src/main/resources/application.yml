ihub:
  application:
    name: client
    port: 8080
  secure:
    client:
      provider-issuer: http://auth-server:9527
      client-id: messaging-client
      client-secret: secret
      scope: message.read,message.write

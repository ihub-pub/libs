plugins {
    id 'pub.ihub.plugin.ihub-settings' version '1.1.1'
}

iHubSettings {
    includeProjects 'ihub-core'      prefix ''   subproject
    includeProjects 'ihub-starter', 'ihub-secure'   prefix ''   subproject      '-spring-boot-starter'
}

plugins {
    id 'pub.ihub.plugin.ihub-settings' version '1.0.12'
}

iHubSettings {
    includeProjects 'ihub-core', 'ihub-common'      prefix ''   subproject()
    includeProjects 'ihub-starter', 'ihub-secure'   prefix ''   subproject      '-spring-boot-starter'
}
